include: package:very_good_analysis/analysis_options.yaml

analyzer:
  plugins:
    - custom_lint
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "lib/l10n/generated/**"
    - build/**
    - android/**
    - ios/**
  errors:
    invalid_annotation_target: ignore
    # custom_lint still hooks in via the legacy analyzer plugin API.
    analysis_options_deprecated_plugins: ignore

linter:
  rules:
    public_member_api_docs: false      # zu streng fuer App-Code
    lines_longer_than_80_chars: false  # 100 ist unser Limit
    prefer_single_quotes: true
    always_use_package_imports: true
    avoid_print: true
